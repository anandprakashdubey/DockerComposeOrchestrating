apiVersion: v1
kind: Service
metadata:
  name: {{ template "pluralsightmoviestoreclient.fullname" . }}
  labels:
    app: {{ template "pluralsightmoviestoreclient.name" . }}
    chart: {{ template "pluralsightmoviestoreclient.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "pluralsightmoviestoreclient.name" . }}
    release: {{ .Release.Name }}
